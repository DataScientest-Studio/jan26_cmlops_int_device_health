apiVersion: v1
kind: Service
metadata:
  name: streamlit
  namespace: mlops
  labels:
    app: streamlit
spec:
  selector:
    app: streamlit
  ports:
    - port: 8501
      targetPort: 8501
  type: ClusterIP
