apiVersion: v1
kind: Service
metadata:
  name: airflow
  namespace: mlops
  labels:
    app: airflow
spec:
  selector:
    app: airflow
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
